spring:
  application:
    name: spring-data
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/springdata?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password:
  jpa:
#    show-sql:true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql:true
